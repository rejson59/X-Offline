{
  "name": "@x-offline/proxy",
  "private": true,
  "version": "0.2.0",
  "type": "module",
  "description": "Cienkie, tylko-do-czytania proxy dla X-Offline: omija CORS i buforuje odpowiedzi.",
  "main": "index.js",
  "scripts": {
    "dev": "node --watch index.js",
    "start": "node index.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20.11"
  }
}
